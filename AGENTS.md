# Repository Instructions

- Run the site from the repository root with `npm run dev`.
- The local server listens on `http://127.0.0.1:3000` by default.
- Override the bind address or port with `HOST` and `PORT` if needed.
- This is a static site. Open pages directly, for example `/`, `/index.html`, or `/experiments/place-cells.html`.
